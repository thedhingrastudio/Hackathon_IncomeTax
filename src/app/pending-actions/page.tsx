import Link from "next/link";
import PageHeading from "../../components/portal/PageHeading";
import RecordDetails from "../../components/portal/RecordDetails";
import { getOutstandingDemand } from "../../data/mock";
import { formatAssessmentYear, formatIndianCurrency, formatIndianDate, formatRecordLabel } from "../../lib/format-tax";
export default function PendingActionsPage() { const demand = getOutstandingDemand(); return <><PageHeading eyebrow="Account actions" title="Pending Actions" description="Review items that require a response from you." /><section className="ux4g-card ux4g-card-outline ux4g-card-vertical demand-card" aria-labelledby="pending-demand-title"><div className="ux4g-card-header demand-card__header"><div><p className="eyebrow">Outstanding Demand</p><h2 id="pending-demand-title">{formatIndianCurrency(demand.amount, demand.currency)}</h2></div><span className="demand-status"><span className="ux4g-badge-icon-warning ux4g-badge-m" aria-hidden="true">!</span><strong>{formatRecordLabel(demand.status)}</strong></span></div><div className="ux4g-card-body demand-card__body"><RecordDetails details={[{ label: "Assessment Year", value: formatAssessmentYear(demand.assessmentYear) }, { label: "Created on", value: formatIndianDate(demand.createdOn) }, { label: "Demand status", value: "Response pending" }]} /></div><div className="ux4g-card-footer demand-card__footer"><Link className="ux4g-btn ux4g-btn-primary ux4g-btn-md" href="/pending-actions/demand">View details</Link></div></section><section className="other-action-areas" aria-labelledby="other-actions-title"><div className="section-heading"><div><p className="eyebrow">Other action areas</p><h2 id="other-actions-title">More pending-action services</h2></div></div><ul><li><strong>Worklist</strong><span>No pending items</span></li><li><strong>e-Proceedings</strong><span>No pending items</span></li><li><strong>Compliance Portal</strong><span>External service</span></li></ul></section></>; }
